todo

0115

1. 筛数据
   1. 确定评测 prompt✅
   2. 将只有一轮对话数据的文件去除 ✅ ->（梳理目前的文件内容）
   3. 跑筛选数据的脚本（开多并发，实现 30rpm）
   4. 编写脚本，根据分数将前 25% 的数据筛选，成为 sft 数据
   5. 0116：将筛选出来的数据修改为 sft 样式，开始 sft 训练；0116 得到训练后的模型，然后进行评测



2. 测基模
   1. 确定推理 prompt，来评测基模的能力
   2. 完善评测脚本
   3. 写一个串行脚本流，模型对话结束后，直接对 450 个任务进行打分



3. 衡量成本，成本太高

   1. 测算筛选数据所需的 token 数

      81 -> 0.48 百万

      17819 -> 100 百万（2～20 轮）

   2. 评测一次，大概需要 20 百万的 token

   3. 疑问：选用 gpt5,还是其他系列

```pytho
python count_turns.py \
  --dialogue-dir /share/liyihan12/projects/NVC/data_create_thread/data_vllm_all/20260113 \
  --episode-file "/share/liyihan12/projects/NVC/data_create/data_input/cleaned_experiment_episodes_dedup_scenario_agents_background.jsonl" \
  --output "./output/count_stats.json"
  
```



0116

1. self play 进行基模推理。4 卡+4 卡
2. 用筛选后的 sft 数据进行 sft 训练。4 卡+4 卡
3. 开发 rl 的奖励函数，调用 235b 模型进行 rl 训练



0117 

1. 对比 gpt 评分和 qwen 打分的结果，得出哪个更客观的结论 -》 决定了打分用什么模型
2. Qwen3 模型进行推理，得到推理结果
3. 结合 1 和 2，对 2 的推理结果进行打分
4. 处理 sft 训练集，推理 prompt 和 2 中保持一致，修改为 sft 的训练格式，查看 max length 的长度
5. 进行 sft 训练
6. 得到训练结果后进行评测



基于我们的 gap，共情、角色扮演，对强化学习算法进行改良



> 完成 1-5 后，可以学习小红书博主的内容





0119 周一

1. 写一个 sft 的 loss 曲线脚本
2. 修改推理的脚本，20 个并发，必须等这20 条数据全部对话结束，再开 20 个并发，这样就不会出现过度拥塞导致数据 error 的情况



1. 基模进行推理
2. sft 模型进行推理
3. 确认评测方式，决定最终采用什么模型来评测
4. 推理之后进行评测

> 得到结论，sft 是否有效



bug：

qwen3-4b 和 8b 在进行 sft 训练时，没有关闭think 模式

改进：对训练数据后增加   /no_think

---

整体流程

- 数据生产
  - 蒸馏：对话双方，分别设计 system prompt ➕ user prompt，增量式 逐标签生成
  - 筛选：对话数据逐条打分
- 训推
  - 推理：system prompt ➕ user prompt
  - 评测：对整个对话历史打分
- sft：采用上方的推理 prompt
- rl：奖励函数打分 prompt





---

