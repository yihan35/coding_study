共情+角色扮演本身就需要一饰多，是MoE类模型比较好的发挥场所，DAPO其实不错，解决了很多实际训练中的问题(类似熵坍塌比较有亮点)，但DAPO其实会加重MoE的负载不均衡，所以MoE模型效果不好，可以继续按GSPO的方向优化，解决他的一些难点，这样把热门的几个top tag都带上；

目前RL主要分类是onpolicy offpolicy；也可以更多结合两者优点，或者做on-policy distillation：结合off policy的初始化优势和on policy的精调效果；

这块大家之前的改动其实也基本都是类似于修改重要性采样，对数据的利用率；

包括优化粒度：token级别or序列级别，这里也可以设置让模型动态决定

(简而言之，要么解决已有的缺点，要么融合现有的有点)

类似GSPO：主要是解耦方面的优势，对每个奖励进行独立判断归一化，再统一协调

(也可以结合共情+角色扮演拆分细粒度的任务)

要么就是类似DPO：绕过交互，直接利用人类偏好，本质上是损失少部分性能换模型效率；主要思路也就三条：

优化速度：提高性价比，提升工业价值

优化效果：提高结果，刷榜or作为teacher的价值

简化：对多个模块进行化简；

每块有得必有失，看你想深挖哪块了，但有个换算比，只要换算是划算的其实就是好的



游戏类的模型，因为共情，最大的特点是，对于不同的人需要的程度及方式也是不同的，可以和游戏领域的方法进行结合

类似meta-RL 让模型拥有学习的能力，适用于游戏AI对不同玩家生成不同风格，类似LAMER学到一种自适应的反思机制

包括长期情感陪伴，人在不同程度的陪伴状态的需求是递进的，比如刚认识，不太熟悉，到后面无话不谈，简而言之，模拟两个正常人认识的状态，通过类似ProRL的方式，延长训练过程，提升模型的丰富程度；





1. 优化训推prompt，优化描述和输出格式，增加 few shot。修改后的推理效果：(正在跑)
2. 查看 sft 数据（是否出现大量重复的数据）【脚本】
3. 新增数据筛选策略，goal 进步较大的对话作为高质量数据，剔除重复性高的数据。数据量：【脚本】
4. 考虑用 moe 模型来训练。







排查问题：

1. 看一下 sft 筛数据是怎么打分的
1. 优化 prompt，训推 prompt：强调根据对话历史和目标，推动对话，不要重复之前说过的内容
1. 对于 sft 数据，必须保证 output 中的标签内容符合规范，prompt 更新后，再开始训练



待解决的问题

1. 优化训推 prompt









背景：在快手用户私信聊天时，向他们提供可能的话题或者辅助聊天的话术，以提升快手用户的私信渗透

技术：收集数据，高质量的快手用户聊天数据







1. 部署模型，推理➕评测
2. 开发 verl 脚本



汇报

1. 下载了英鹏开发的脚本，用 vllm 部署一个模型，将 sotopia 原本的信息进行提取，转换成 social sim 推理需要的格式和内容，进行推理➕评测
2. 开发 verl 脚本，周末进行 rl 训练，奖励分为目标，共情，角色三个维度，加权；格式奖励
3. 清洗 sft 的训练数据：方案：
   1. 逐句检测该句是否和上一轮次的话，说法相似、语义重复 没有新增信息
   2. 
   3. 推理 prompt 中强调禁止重复









