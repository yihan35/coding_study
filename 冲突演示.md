# Git 冲突实战演练

## 🎮 动手实践：体验分支冲突

### 第一步：创建练习环境

```bash
# 创建一个练习目录
mkdir ~/git-conflict-demo
cd ~/git-conflict-demo

# 初始化 Git
git init

# 创建一个 Python 文件
cat > calculator.py << 'EOF'
def add(a, b):
    result = a + b
    print("结果是：", result)
    return result

def subtract(a, b):
    return a - b
EOF

# 提交初始版本
git add calculator.py
git commit -m "初始版本"
```

### 第二步：创建第一个分支并修改

```bash
# 创建分支：改进打印格式
git checkout -b improve-print

# 修改文件（用编辑器打开 calculator.py）
# 把第3行改成：
#     print(f"加法结果：{result}")

# 或者用命令直接修改
cat > calculator.py << 'EOF'
def add(a, b):
    result = a + b
    print(f"加法结果：{result}")
    return result

def subtract(a, b):
    return a - b
EOF

# 提交修改
git add calculator.py
git commit -m "改进打印格式"
```

### 第三步：回到 main 创建另一个分支

```bash
# 切回 main（回到初始状态）
git checkout main

# 创建另一个分支：添加日志
git checkout -b add-logging

# 修改同一行（第3行）
cat > calculator.py << 'EOF'
def add(a, b):
    result = a + b
    print("[LOG]", a, "+", b, "=", result)
    return result

def subtract(a, b):
    return a - b
EOF

# 提交修改
git add calculator.py
git commit -m "添加日志功能"
```

### 第四步：制造冲突！

```bash
# 回到 main
git checkout main

# 先合并第一个分支（成功）
git merge improve-print
# ✅ 输出：Fast-forward（快速合并）

# 再合并第二个分支（冲突！）
git merge add-logging
# ❌ 输出：CONFLICT (content): Merge conflict in calculator.py
```

### 第五步：查看冲突

```bash
# 查看状态
git status
# 会显示：both modified: calculator.py

# 查看冲突内容
cat calculator.py
```

你会看到：

```python
def add(a, b):
    result = a + b
<<<<<<< HEAD
    print(f"加法结果：{result}")
=======
    print("[LOG]", a, "+", b, "=", result)
>>>>>>> add-logging
    return result

def subtract(a, b):
    return a - b
```

### 第六步：解决冲突

**方法A：手动编辑文件**

```bash
# 用编辑器打开 calculator.py，改成：
def add(a, b):
    result = a + b
    print(f"[LOG] 加法结果：{result}")  # 合并两个想法
    return result

def subtract(a, b):
    return a - b

# 保存文件
```

**方法B：使用 Git 命令选择**

```bash
# 保留当前分支的版本（HEAD）
git checkout --ours calculator.py

# 或保留要合并的分支版本
git checkout --theirs calculator.py
```

### 第七步：完成合并

```bash
# 标记冲突已解决
git add calculator.py

# 查看状态
git status
# 显示：All conflicts fixed but you are still merging

# 完成合并
git commit -m "解决冲突：合并打印改进和日志功能"

# 查看历史
git log --graph --oneline --all
```

---

## 📊 冲突类型对照表

| 场景 | 会冲突吗？ | 说明 |
|------|----------|------|
| 两个分支修改同一行 | ✅ 会 | 必须手动选择 |
| 两个分支修改相邻行 | ⚠️ 可能 | Git 会尝试自动合并 |
| 修改不同文件 | ❌ 不会 | 自动合并 |
| 修改同文件不同函数 | ❌ 不会 | 自动合并 |
| 同名变量在不同位置 | ❌ 不会 | 变量名不影响 |
| A修改，B删除同一行 | ✅ 会 | 需要决定删还是留 |
| 新增同名文件且内容不同 | ✅ 会 | 需要选择哪个文件 |
| 新增同名文件且内容相同 | ❌ 不会 | Git 识别为相同内容 |

---

## 🔧 解决冲突的工具

### 1. VS Code（推荐）

VS Code 会自动识别冲突，显示：

```
◆ Accept Current Change（接受当前更改）
◆ Accept Incoming Change（接受传入更改）
◆ Accept Both Changes（接受双方更改）
◆ Compare Changes（比较更改）
```

只需点击按钮即可！

### 2. 命令行工具

```bash
# 使用 Git 自带的合并工具
git mergetool

# 查看冲突文件列表
git diff --name-only --diff-filter=U

# 取消合并（放弃解决冲突）
git merge --abort
```

### 3. 图形化工具

- **GitKraken**：可视化分支图
- **SourceTree**：直观的冲突解决界面
- **GitHub Desktop**：适合初学者

---

## 💡 避免冲突的最佳实践

### 1. 频繁同步

```bash
# 每天开始工作前
git checkout main
git pull

# 定期把 main 的更新合并到自己的分支
git checkout my-feature
git merge main
```

### 2. 小步提交

```bash
# ❌ 不好：一周后提交一次
git commit -m "完成了所有功能"

# ✅ 好：每完成一小部分就提交
git commit -m "完成登录表单UI"
git commit -m "添加表单验证"
git commit -m "连接登录API"
```

### 3. 分工明确

- 小明负责 `auth/` 目录
- 小红负责 `payment/` 目录
- 减少修改同一文件的机会

### 4. 及时沟通

```bash
# 如果要改公共文件，先告诉队友
# "我要改 utils.py 的第50行，你在用吗？"
```

---

## 🎯 常见问题

### Q1: 冲突了怎么办，会丢代码吗？

**不会丢！** Git 会保留所有版本：
- `<<<<<<< HEAD` 里是一个版本
- `=======` 下面是另一个版本
- 你可以手动合并，甚至两个都要

### Q2: 我不想解决冲突，能撤销吗？

```bash
# 取消这次合并
git merge --abort

# 回到合并前的状态
```

### Q3: 解决冲突后需要测试吗？

**必须测试！**
- 合并后代码可能语法正确，但逻辑错误
- 举例：小明加了 `x + 1`，小红加了 `x * 2`，你合并成 `x + 1 * 2`，运算顺序就错了

### Q4: 变量重名会冲突吗？

**只看位置**：
```python
# main 分支
def func1():
    count = 0  # 第2行

# feature 分支
def func2():
    count = 0  # 第20行

# ✅ 不冲突！位置不同
```

但如果：
```python
# main 分支
def calculate():
    result = x + y  # 第5行

# feature 分支
def calculate():
    result = x * y  # 第5行（同一位置）

# ❌ 冲突！同一行
```

---

## 🏆 冲突解决检查清单

合并后务必检查：

- [ ] 删除了所有 `<<<<<<<`、`=======`、`>>>>>>>` 标记
- [ ] 代码能正常运行（没有语法错误）
- [ ] 逻辑符合预期（合并的逻辑是对的）
- [ ] 运行测试（如果有）
- [ ] 提交前再次 review 代码

```bash
# 解决冲突后的标准流程
git add <冲突文件>
git commit -m "解决冲突：<说明如何解决的>"
git push
```

---

## 🎓 总结

**冲突的本质**：
- Git 按行跟踪代码
- 两个人改了同一行 → Git 不知道听谁的
- 需要人工判断

**变量重名不是问题**：
- Git 不关心变量名
- 只关心**哪一行的代码**被修改了

**解决冲突的心态**：
- 不要怕！冲突很正常
- 就像两个人同时说话，需要协调一下而已
- 代码不会丢失，放心解决
